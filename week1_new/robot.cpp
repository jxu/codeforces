#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

typedef pair<int,int> pii;


int main() 
{
    vector<pii> pos;
    
    int x=0, y=0;
    pos.push_back(pii(0,0));

    char c;
    while (cin >> c) {
        if (c == 'L') {
            --x; pos
            
            pos += pii(-1, 0);

        }
    }
}
